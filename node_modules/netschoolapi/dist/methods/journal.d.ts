import NS from "../NetSchoolApi-safe.js";
import Journal from "../classes/Journal";
export interface Credentials {
    start?: Date;
    end?: Date;
    classId?: number;
    studentId?: number;
}
export default function journal(this: NS, credentials?: Credentials): Promise<Journal>;
