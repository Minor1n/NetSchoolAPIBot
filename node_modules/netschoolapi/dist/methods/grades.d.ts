import NS from "../NetSchoolApi-safe.js";
import Grades from "../classes/Grades";
export interface Credentials {
    subjectId: number;
    start?: Date;
    end?: Date;
    classId?: number;
    studentId?: number;
}
export default function grades(this: NS, credentials: Credentials): Promise<Grades>;
