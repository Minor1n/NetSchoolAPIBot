"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
/**
 * Преобразование строки в объект
 * @param cookie Значение заголовка `set-cookie`
 */
function default_1(cookie) {
    return cookie === null || cookie === void 0 ? void 0 : cookie.map((cookie) => cookie.substring(0, cookie.indexOf(";"))).map((cookie) => {
        const i = cookie.indexOf("=");
        const key = cookie.substring(0, i);
        const val = cookie.substring(i + 1);
        return { key, val };
    });
}
exports.default = default_1;
//# sourceMappingURL=decode.js.map